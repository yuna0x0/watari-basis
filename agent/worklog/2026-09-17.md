# 2026-09-17

## A copy built by VRCFury reads as an avatar without a menu

A user exported a VRCFury Editor Test Copy into a Basis project and got no toggles. Read from
VRCFury's source: the copy's controllers, clips, menus and parameters are written under
`Packages/com.vrcfury.temp/Builds/<avatar>`, which a unitypackage export leaves out; and the menus
and parameters are attached as sub-assets of a `BinaryContainer` marked `PreferBinarySerialization`,
so that file is binary whatever the project's setting. Unity does not re-save an asset whose
script is missing (checked: `ForceReserializeAssets` on a missing-script menu left the file byte
for byte), so nothing in a Basis project can turn that container into text. Reading it is not
possible here; the original avatar is what converts.

Two things were wrong on this side. The loader took the first script document in a file, so a
menu packed into a container with other assets could never be read even as text; it reads by file
id now, and the descriptor and submenu references carry theirs. And an unreadable or absent
menu, parameter or controller asset was silent; each is a warning naming the cause. On the
branch `vrcfury-build-output`, unreleased.

VRCFury ships no sample avatar. One prefab on this machine carries its components: an Armature
Link, a Blendshape Link and a Full Controller, serialized as managed references under `content`
and `references.RefIds` with a `type: {class, ns, asm}` per entry. A reader for those is a source
of its own, the size of the Modular Avatar one, and needs real content with Toggle features to
check against; none is at hand.

Later the same day: VRCFury's component and its build marker are known identities now. The
component is reported as not read; the marker, which only a built copy carries, names the copy
for what it is. A menu reference whose file id is not the main asset is called out as packed,
since that is how VRCFury saves a built copy's menus.

## VRCFury read as a source

Toggles and Full Controllers are read from VRCFury components on the original avatar and fed
into the same `ResolvedToggle` records the Modular Avatar path produces, so the Vixxy mapper and
writer did not change. What was built is what the public corpus shows in use (Koda-Winterpaw,
1028 components): object, blendshape and material property actions; Full Controllers with FX
controllers, menus with a prefix, parameters and binding rewrites; every schema version present
(Toggle 3, FullController 4, ArmatureLink 6 and 7) with VRCFury's own upgrade rules; the older
`config.features` list layout, one component in ten. The off side of an object action is
explicit because VRCFury applies the off clip to the resting state (`RestingStateService.
ApplyImplicitRestingStates`); every other action's off side is the rest pose. Armature Link is
read but not applied: VRCFury runs only under VRChat's build, so unlike Modular Avatar nothing
moves the bones on Basis, and a writer for that is the next piece. A nested YAML block helper
was needed for the managed-reference layout. Checked against VRCFury 1.1429.0.

## Armature Link applied

The one VRCFury feature with no machinery on Basis. `ArmatureLinkPlanner` reproduces VRCFury's
`GetLinks`: the target by humanoid bone with the humanoid parent chain as fallback, by object or
by path; children matched by name with the named or implied suffix removed and the four mid-bone
fixups; bones under a PhysBone left with their chain unless the avatar bone is humanoid; the scale
factor as `GetScalingFactor`; the old Auto mode decided by `HasExternalSkinBoneReference` on the
clothing's skins. `ArmatureLinkWriter` is the child-merge form only: reparent under the avatar
bone with world pose kept, align as asked, rename `[VF] <bone> from <clothing>`, and skip a bone
already under its target so converting twice moves nothing. VRCFury's skin rewrite and pruning
change bone count, not where clothing sits, and are left out; its toggle wrappers for animated
parents are replaced by a report line when a linked bone carries renderers. Bones move before
every other writer. The Auto-mode path has no fixture of its own; the corpus check covers its
reading and the writer path is shared. New target row, `Armature links`. The first run renamed
the bones and moved nothing: Unity refuses to let an object leave its prefab instance, which is
why VRCFury and Modular Avatar work on build clones. The writer unpacks the instance a bone
belongs to, outermost root only, as many times as needed, in the same undo group, and the result
says so as `apply.unpacked`; the avatar's own instance is untouched. Converting again then reads
the clothing no more, which is the honest consequence and is documented.

## Components kept on a prefab of their own

One reporter keeps the armature and meshes clean and prefabs every VRChat component, so the
descriptor sits on a nested prefab and each PhysBone names its root bone through an override the
outer prefab stores, pointing at the outer file's own object. A fixture in that shape showed the
descriptor found and the PhysBone dropped as `physbone.rootUnresolved`: the override was written
into the nested document as a bare id the nested file's resolver could not know. Now an override
carries the guid of the file it came from, a reference that crosses files is rewritten to an id the
plan owns, and each file's resolver hands such ids back to the plan, which resolves them through the
origin file's resolver. Every reference kind gets this without touching a reader. Colliders are
indexed by file and id for the same reason. A second gap surfaced on the way: a list that was empty
in the nested file could not be grown by an override, so colliders assigned to a nested PhysBone
were dropped; an empty list is seeded with empty references now. 325 tests.

## Next

0.8.5 is the stop line. The branch waits for the user who reported it to confirm what it says.
