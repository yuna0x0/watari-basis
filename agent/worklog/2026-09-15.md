# 2026-09-15

## Synced the clone to current Basis

Three upstream commits: moderation and permissions on the server, exception dialogs, the UI
language files, a far-LOD bake threaded through the bundle pipeline, and the Unity analytics
module dropped from the manifest. Nothing Watari reads or writes moved: no change in jiggle
physics, the SDK's avatar components, the constraint or head chop types, the eye, lip and rig
drivers, or Vixxy's serialized shape. Suite 295 green; versions row raised.

## Next

0.8.3 is the stop line for Watari. Content lives outside the Basis clone; the clone tracks
upstream.
