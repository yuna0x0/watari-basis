# 2026-09-08

## Basis sync

Synced the clone to upstream `developer` at c7d7bc44a and ran the suite against it: 262 green,
no compile errors.

- Upstream's changes to the packages Watari references (`com.basis.sdk`, jiggle physics,
  `HVR.Basis.Comms`) do not touch anything Watari reads or writes: `HVRVixxyOrchestrator.VariableStore`
  became non-serialized and `HVRVixxySetup` dropped a `FindObjectsSortMode` argument. The lip
  sync driver moved inference to its own thread with the viseme set unchanged; the height driver
  gained a timed measurement window. No package change.
- `versions.md` now names the upstream commit id, linked, and the date in the Basis row.
- `AGENTS.md` gained "Versions checked": the page and `ProductInfo.CheckedAgainst` are updated in
  the same change as every tested sync or source package upgrade. `CONTRIBUTING.md` points at it
  from Tests.

## Next

Unchanged: the four images, the two jiggle heuristics, Magica Cloth 2.
