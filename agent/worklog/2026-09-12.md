# 2026-09-12

## Basis sync

Synced the clone to upstream `developer` at 748fd4d3b and ran the suite against it: 262 green,
no compile errors.

- Twelve upstream commits, mostly full-body IK (arm, shoulder, wrist and knee solves in
  `com.basis.eeriemovement` and `BasisLocalRigDriver`), the hand-held camera, MediaPipe, the
  server, and a new `com.basis.shim` package. The packages Watari references (`com.basis.sdk`,
  jiggle physics, `HVR.Basis.Comms`) changed only in fonts, localization strings, the logo and
  icon guids in `.meta` files. `BasisTransformMapping.FindTwistBone`, the eye driver and the lip
  sync driver are unchanged, and the required bone set the rig check mirrors did not move.
- The avatar content allow-list gained `BasisPlatformSwitch` and `BasisPlatformEventShim` and
  dropped nothing.
- `versions.md` Basis row raised to 748fd4d3b. No package change.
- Considered and rejected: writing `BasisPlatformSwitch` (new, enables or disables objects by
  device class such as mobile GPU or Android). No source carries that data. VRChat's per-platform
  overrides name a whole separate avatar per platform, Modular Avatar's platform filter selects
  by NDMF build platform (VRChat, Resonite) at build time, and VRM has nothing. The Cilbox
  platform and graphics shims are a scripting API with no equivalent in any source.

## Next

Unchanged: the four images, the two jiggle heuristics, Magica Cloth 2.
